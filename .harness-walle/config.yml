# Walle consumer config. Modules mirror .harness-walle/manifest.json (authoritative).
# Add/remove modules with 'walle add <module>' or the init flags, not by hand.
modules:
  - website
  - ai
  - ci
# Copy the walle wiki into .harness-walle/docs/ on update. Set false to skip.
docs: true
