---
import NavbarMenu from "./NavbarMenu.astro";
import type { NavbarConfig } from "@walle/config";
import { resolveInternalUrl } from "@walle/utils";

export interface Props {
  config: NavbarConfig;
}

const { config } = Astro.props;
const navId = `nav-${Math.random().toString(36).substring(2, 9)}`;

const hasLogo = config.logo?.src;
const hasTitle = config.logo?.title;
const shouldShowBrand = hasLogo || hasTitle;
---

<header class="site-header">
  <div class="container">
    <nav class="main-nav" aria-label="Main navigation" data-component="Navbar" data-nav-root="true">
      <slot name="brand">
        {
          shouldShowBrand && (
            <div class="brand-container">
              <a
                href={resolveInternalUrl(config.logo?.url || "/")}
                title={config.logo?.title || "Home"}
                class="brand-link"
              >
                {hasLogo ? (
                  <img
                    src={config.logo.src}
                    alt={config.logo.alt || config.logo.title || "Logo"}
                    aria-label={config.logo.alt || config.logo.title || "Logo"}
                    width={config.logo.width || 50}
                    height={config.logo.height || 50}
                    loading="eager"
                    class:list={["brand-logo", config.logo.cssClasses]}
                    fetchpriority="high"
                  />
                ) : hasTitle ? (
                  <span class:list={["brand-title", config.logo.cssClasses]}>
                    {config.logo.title}
                  </span>
                ) : null}
              </a>
            </div>
          )
        }
      </slot>

      <NavbarMenu items={config.items || []} navId={navId} />

      <slot name="actions" />

      {/* Last in the DOM so on mobile the reading/focus order matches the visual order
          (brand · cart · hamburger) without CSS `order` reshuffling the tab sequence. */}
      <button
        id="mobile-menu-toggle"
        class="hamburger"
        aria-controls={navId}
        aria-expanded="false"
        aria-label="Toggle navigation menu"
      >
        <span class="hamburger-line"></span>
        <span class="hamburger-line"></span>
        <span class="hamburger-line"></span>
      </button>
    </nav>
  </div>
</header>

<style is:global>
  .site-header {
    position: sticky;
    top: 0;
    width: 100%;
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border-bottom: 1px solid rgba(0, 0, 0, 0.08);
    z-index: 1000;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .site-header.scrolled {
    background: rgba(255, 255, 255, 0.98);
    border-bottom-color: rgba(0, 0, 0, 0.12);
    box-shadow: 0 4px 32px rgba(0, 0, 0, 0.08);
  }

  .site-header .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1rem;
  }

  .main-nav {
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 70px;
    position: relative;
  }

  .brand-container {
    display: flex;
    align-items: center;
    z-index: 100;
    flex-shrink: 0;
  }

  .brand-link {
    display: flex;
    align-items: center;
    text-decoration: none;
    transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
    outline: none;
  }

  .brand-link:focus {
    outline: none;
    box-shadow: none;
  }

  /* Text logo: a wordmark, not a section heading (kept out of the heading outline).
     `.brand-title` is the tuning knob for its size. */
  .brand-link .brand-title {
    display: inline-block;
    margin: 0;
    font-size: 1.4rem;
    font-weight: 700;
    line-height: 1;
    letter-spacing: -0.02em;
    color: var(--primary);
  }

  /* Image logo default: sizes reliably for both raster and SVG (SVGs often lack
     intrinsic dimensions, so we constrain by height and let width follow). Height
     is a token knob; consumers extend/override via the logo `cssClasses`. */
  .brand-link .brand-logo {
    display: block;
    height: var(--navbar-logo-height, 40px);
    width: auto;
    max-width: 180px;
    object-fit: contain;
  }

  @media (max-width: 480px) {
    .brand-link .brand-title {
      font-size: 1.2rem;
    }
    .brand-link .brand-logo {
      height: var(--navbar-logo-height-mobile, 34px);
    }
  }

  .nav-container {
    display: flex;
    align-items: center;
    margin-left: auto;
  }

  .nav-links {
    display: flex;
    list-style: none;
    margin: 0;
    padding: 0;
    gap: 0.5rem;
    align-items: center;
  }

  .nav-item {
    position: relative;
  }

  .nav-link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1.25rem;
    color: var(--black, #1a1a1a);
    text-decoration: none;
    font-size: 0.95rem;
    font-weight: 500;
    background: none;
    border: none;
    cursor: pointer;
    font-family: inherit;
    border-radius: var(--radius-md);
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    letter-spacing: -0.01em;
    outline: none;
  }

  .nav-link:focus,
  .nav-link:active {
    outline: none;
    box-shadow: none;
    border: none;
  }

  @media (min-width: 769px) {
    .nav-link::after {
      content: "";
      position: absolute;
      bottom: 6px;
      left: 50%;
      width: 0;
      height: 2px;
      background: var(--primary);
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      transform: translateX(-50%);
      border-radius: 1px;
    }

    .nav-link:hover::after {
      width: calc(100% - 2.5rem);
    }

    .nav-link:hover {
      color: var(--primary);
      transform: translateY(-1px);
    }
  }

  .dropdown-trigger .dropdown-arrow {
    transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    margin-left: 0.25rem;
    opacity: 0.7;
  }

  .nav-item.has-dropdown:hover .dropdown-trigger .dropdown-arrow,
  .nav-item.has-dropdown.active .dropdown-trigger .dropdown-arrow {
    transform: rotate(180deg);
    opacity: 1;
  }

  .dropdown-container {
    position: relative;
  }

  .dropdown-menu {
    position: absolute;
    top: calc(100% + 12px);
    left: 50%;
    transform: translateX(-50%) translateY(-8px) scale(0.95);
    min-width: 160px;
    width: max-content;
    max-width: min(calc(100vw - 2rem), 520px);
    background: white;
    border-radius: var(--radius-lg);
    box-shadow: 0 12px 48px rgba(0, 0, 0, 0.15);
    opacity: 0;
    visibility: hidden;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    z-index: 1000;
    list-style: none;
    margin: 0;
    padding: 12px;
    border: 1px solid rgba(0, 0, 0, 0.06);
    box-sizing: border-box;
    overflow-wrap: anywhere;
  }

  .nav-item.has-dropdown:hover .dropdown-menu,
  .nav-item.has-dropdown.active .dropdown-menu {
    opacity: 1;
    visibility: visible;
    transform: translateX(-50%) translateY(0) scale(1);
  }

  .dropdown-item {
    position: relative;
  }

  .dropdown-link {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.875rem 1.25rem;
    color: var(--black, #1a1a1a);
    text-decoration: none;
    font-size: 0.9rem;
    font-weight: 500;
    background: none;
    border: none;
    cursor: pointer;
    font-family: inherit;
    width: 100%;
    text-align: left;
    border-radius: var(--radius-md);
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    letter-spacing: -0.01em;
    outline: none;
  }

  .dropdown-link:hover,
  .dropdown-link:focus {
    color: var(--primary);
    transform: translateX(4px);
    outline: none;
    box-shadow: none;
  }

  .hamburger {
    display: none;
    background: none;
    border: none;
    cursor: pointer;
    padding: 10px;
    z-index: 100;
    width: 48px;
    height: 48px;
    justify-content: center;
    align-items: center;
    position: relative;
    border-radius: var(--radius-md);
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .hamburger:hover {
    background-color: color-mix(in srgb, var(--primary) 8%, transparent);
  }

  .hamburger-line {
    display: block;
    width: 24px;
    height: 2px;
    background-color: var(--black, #1a1a1a);
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    position: absolute;
    border-radius: 2px;
  }

  .hamburger-line:nth-child(1) {
    transform: translateY(-6px);
  }
  .hamburger-line:nth-child(2) {
    transform: translateY(0);
  }
  .hamburger-line:nth-child(3) {
    transform: translateY(6px);
  }

  .hamburger.active .hamburger-line:nth-child(1) {
    transform: rotate(45deg);
  }
  .hamburger.active .hamburger-line:nth-child(2) {
    opacity: 0;
    transform: scale(0);
  }
  .hamburger.active .hamburger-line:nth-child(3) {
    transform: rotate(-45deg);
  }

  @media (max-width: 768px) {
    .site-header .container {
      padding: 0 1rem;
    }

    .main-nav {
      height: 64px;
    }

    /* Right cluster on mobile: brand stays left; the cart + hamburger sit together on the
       right (cart immediately left of the hamburger). The nav overlay (.nav-container) is
       fixed/out-of-flow, and DOM order is already brand · cart · hamburger, so `margin-left:
       auto` pushes the pair right without CSS `order` — keeping tab order == visual order. */
    .hamburger {
      display: flex;
      margin-left: 0.5rem;
    }

    .cart-badge--inline {
      margin-left: auto;
    }

    .nav-container {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100vh;
      height: 100dvh;
      background: rgba(255, 255, 255, 0.98);
      backdrop-filter: blur(24px);
      -webkit-backdrop-filter: blur(24px);
      visibility: hidden;
      opacity: 0;
      transform: scale(0.95);
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      display: flex;
      flex-direction: column;
      justify-content: flex-start;
      align-items: stretch;
      padding: 80px 0 40px 0;
      z-index: 90;
      overflow-y: auto;
      overflow-x: hidden;
      -webkit-overflow-scrolling: touch;
      box-sizing: border-box;
    }

    .nav-container.show {
      visibility: visible;
      opacity: 1;
      transform: scale(1);
    }

    .nav-links {
      flex-direction: column;
      width: 100%;
      gap: 8px;
      padding: 0 1.5rem;
      align-items: stretch;
      box-sizing: border-box;
    }

    .nav-item {
      width: 100%;
      border-bottom: 1px solid rgba(0, 0, 0, 0.08);
      box-sizing: border-box;
    }

    .nav-item:last-child {
      border-bottom: none;
    }

    .nav-link {
      padding: 1.25rem 0.5rem;
      font-size: 1.1rem;
      font-weight: 400;
      border-radius: 0;
      background: transparent;
      letter-spacing: -0.02em;
      display: flex;
      width: 100%;
      align-items: center;
      justify-content: space-between;
      box-sizing: border-box;
    }

    .nav-link:hover {
      background: transparent !important;
      color: var(--primary) !important;
      transform: none !important;
    }

    .nav-link::after {
      display: none;
    }

    .dropdown-trigger .dropdown-arrow {
      opacity: 0.5;
      margin-left: auto;
    }

    .nav-item:not(.active) .dropdown-trigger {
      color: var(--black, #1a1a1a) !important;
      background: transparent !important;
    }

    .nav-item:not(.active) .dropdown-trigger .dropdown-arrow {
      transform: rotate(0deg) !important;
      opacity: 0.5 !important;
    }

    .dropdown-trigger:hover,
    .dropdown-trigger:focus,
    .dropdown-trigger:active {
      color: var(--primary) !important;
      background: transparent !important;
    }

    .dropdown-menu {
      position: static;
      left: auto;
      width: 100%;
      background: transparent;
      box-shadow: none;
      border: none;
      visibility: visible;
      transform: none;
      padding: 0;
      margin: 0;
      border-radius: 0;
      display: block;
      max-height: 0;
      opacity: 0;
      overflow: hidden;
      transition:
        max-height 0.35s cubic-bezier(0.4, 0, 0.2, 1),
        opacity 0.25s ease;
    }

    .nav-item.has-dropdown.active .dropdown-menu {
      max-height: 60vh;
      opacity: 1;
      transform: none;
    }

    .dropdown-item {
      border-bottom: 1px solid rgba(0, 0, 0, 0.04);
    }

    .dropdown-item:last-child {
      border-bottom: none;
    }

    .dropdown-link {
      padding: 1rem 1.5rem;
      font-size: 1rem;
      font-weight: 400;
      color: rgba(0, 0, 0, 0.7);
      background: transparent;
      border-radius: 0;
      letter-spacing: -0.02em;
    }

    .dropdown-link:hover {
      background: transparent;
      color: var(--primary);
      transform: none;
    }

    body.menu-open {
      overflow: hidden;
      position: fixed;
      width: 100%;
      height: 100vh;
      height: 100dvh;
    }
  }

  @media (max-width: 480px) {
    .site-header .container {
      padding: 0 0.75rem;
    }

    .nav-links {
      padding: 0 1rem;
    }

    .nav-link {
      padding: 1.125rem 0.25rem;
      font-size: 1.05rem;
    }

    .dropdown-link {
      padding: 0.875rem 1.25rem;
      font-size: 0.95rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .site-header,
    .nav-link,
    .dropdown-menu,
    .dropdown-link,
    .hamburger,
    .hamburger-line,
    .nav-container {
      transition-duration: 0.01ms !important;
      animation-duration: 0.01ms !important;
    }
  }
</style>

<script>
  import "@walle/scripts/navbar.ts";
</script>
